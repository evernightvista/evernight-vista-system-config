/* Allow users in wheel group to Plasma Login without authentication */
polkit.addRule(function(action, subject) {
    if (action.id == "org.kde.kcontrol.kcmplasmalogin.save" &&
        subject.isInGroup("wheel")) {
        return polkit.Result.YES;
    }
});
