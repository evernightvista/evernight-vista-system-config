/* Allow users in wheel group to run Admin Kio without authentication */
polkit.addRule(function(action, subject) {
    if (action.id == "org.kde.kio.admin.commands" &&
        subject.isInGroup("wheel")) {
        return polkit.Result.AUTH_ADMIN_KEEP;
    }
});
